{{- /*
Generated from 'wiztelemetry-kunlunxin-xpu.rules' group from file://../../../ks-prometheus/manifests/wiztelemetry-prometheusRule.yaml
Do not change in-place! In order to change this file first read following link:
https://github.com/prometheus-community/helm-charts/tree/main/charts/kube-prometheus-stack/hack
*/ -}}
{{- $kubeTargetVersion := default .Capabilities.KubeVersion.GitVersion .Values.kubeTargetVersionOverride }}
{{- if and (semverCompare ">=1.14.0-0" $kubeTargetVersion) (semverCompare "<9.9.9-9" $kubeTargetVersion) .Values.defaultRules.create .Values.defaultRules.rules.gpuDevice.kunlunxinXPU }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ printf "%s-%s" (include "wiztelemetry-monitoring-helper.fullname" .) "wiztelemetry-kunlunxin-xpu.rules" | trunc 63 | trimSuffix "-" }}
  namespace: {{ template "wiztelemetry-monitoring-helper.namespace" . }}
  labels:
    app: {{ template "wiztelemetry-monitoring-helper.name" . }}
{{ include "wiztelemetry-monitoring-helper.labels" . | indent 4 }}
{{- if .Values.defaultRules.labels }}
{{ toYaml .Values.defaultRules.labels | indent 4 }}
{{- end }}
{{- if .Values.defaultRules.annotations }}
  annotations:
{{ toYaml .Values.defaultRules.annotations | indent 4 }}
{{- end }}
spec:
  groups:
  - name: wiztelemetry-kunlunxin-xpu.rules
    rules:
    - expr: |-
        sum by ({{ range $.Values.defaultRules.additionalAggregationLabels }}{{ . }},{{ end }}cluster, namespace, pod, container, node) (
            label_replace(
              label_replace(
                node_container_xpu_utilization{container_name!="",job="xpu-exporter",pod_name!=""} / 100,
                "container",
                "$1",
                "container_name",
                "(.*)"
              ),
              "pod",
              "$1",
              "pod_name",
              "(.*)"
            )
        )
      record: node_namespace_pod_container:container_gpu_utilization
      {{- if or .Values.defaultRules.additionalRuleLabels .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
      labels:
        {{- with .Values.defaultRules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
    - expr: |-
        sum by ({{ range $.Values.defaultRules.additionalAggregationLabels }}{{ . }},{{ end }}cluster, namespace, pod, container, node) (
            label_replace(
              label_replace(
                node_container_xpu_memused{container_name!="",exported_namespace!="",job="xpu-exporter",pod_name!=""} * 1024 * 1024,
                "container",
                "$1",
                "container_name",
                "(.*)"
              ),
              "pod",
              "$1",
              "pod_name",
              "(.*)"
            )
        )
      record: node_namespace_pod_container:container_gpu_memory_usage
      {{- if or .Values.defaultRules.additionalRuleLabels .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
      labels:
        {{- with .Values.defaultRules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
    - expr: |-
        label_replace(
          label_replace(node_xpu_temp{job="xpu-exporter"}, "device_num", "xpu${1}", "devid", "(.*)"),
          "device_name",
          "$1",
          "type",
          "(.*)"
        )
      record: node:gpu_device:gpu_temperature
      {{- if or .Values.defaultRules.additionalRuleLabels .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
      labels:
        {{- with .Values.defaultRules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
    - expr: |-
        label_replace(
          label_replace(node_xpu_powerUsage{job="xpu-exporter"}, "device_num", "xpu${1}", "devid", "(.*)"),
          "device_name",
          "$1",
          "type",
          "(.*)"
        )
      record: node:gpu_device:gpu_power_usage
      {{- if or .Values.defaultRules.additionalRuleLabels .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
      labels:
        {{- with .Values.defaultRules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
    - expr: |-
        label_replace(
          label_replace(node_xpu_memused{job="xpu-exporter"}, "device_num", "xpu${1}", "devid", "(.*)") * 1024 * 1024,
          "device_name",
          "$1",
          "type",
          "(.*)"
        )
      record: node:gpu_device:gpu_memory_used_bytes
      {{- if or .Values.defaultRules.additionalRuleLabels .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
      labels:
        {{- with .Values.defaultRules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
    - expr: |-
        label_replace(
          label_replace(node_xpu_memtotal{job="xpu-exporter"}, "device_num", "xpu${1}", "devid", "(.*)") * 1024 * 1024,
          "device_name",
          "$1",
          "type",
          "(.*)"
        )
      record: node:gpu_device:gpu_memory_total_bytes
      {{- if or .Values.defaultRules.additionalRuleLabels .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
      labels:
        {{- with .Values.defaultRules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
    - expr: |-
        label_replace(
          label_replace(
            node_xpu_memused{job="xpu-exporter"} / node_xpu_memtotal{job="xpu-exporter"},
            "device_num",
            "xpu${1}",
            "devid",
            "(.*)"
          ),
          "device_name",
          "$1",
          "type",
          "(.*)"
        )
      record: node:gpu_device:gpu_memory_utilization
      {{- if or .Values.defaultRules.additionalRuleLabels .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
      labels:
        {{- with .Values.defaultRules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
    - expr: |-
        label_replace(
          label_replace(node_xpu_utilization{job="xpu-exporter"}, "device_num", "xpu${1}", "devid", "(.*)") / 100,
          "device_name",
          "$1",
          "type",
          "(.*)"
        )
      record: node:gpu_device:gpu_utilization
      {{- if or .Values.defaultRules.additionalRuleLabels .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
      labels:
        {{- with .Values.defaultRules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
    - expr: |-
        sum by ({{ range $.Values.defaultRules.additionalAggregationLabels }}{{ . }},{{ end }}cluster, node) (
          kube_pod_container_resource_requests{job="kube-state-metrics",resource=~"kunlunxin_com_xpu"}
        )
      record: node:node_gpu_allocated_num:sum
      {{- if or .Values.defaultRules.additionalRuleLabels .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
      labels:
        {{- with .Values.defaultRules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
    - expr: |-
        sum by ({{ range $.Values.defaultRules.additionalAggregationLabels }}{{ . }},{{ end }}cluster, node) (
            kube_node_status_allocatable{job="kube-state-metrics",resource=~"kunlunxin_com_xpu"}
        )
      record: node:node_gpu_num:sum
      {{- if or .Values.defaultRules.additionalRuleLabels .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
      labels:
        {{- with .Values.defaultRules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.defaultRules.additionalRuleGroupLabels.gpuDevice.kunlunxinXPU }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
{{- end }}